<resources>

    <!-- ========================================== -->
    <!--                   Themes                   -->
    <!-- ========================================== -->

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ========================================== -->
    <!--               Text Appearances             -->
    <!-- ========================================== -->

    <style name="TextAppearance.MovieToolbar.Collapsed" parent="android:TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TextAppearance.MovieToolbar.Expanded" parent="android:TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:textSize">28sp</item>
    </style>

    <!-- ========================================== -->
    <!--                    UI's                    -->
    <!-- ========================================== -->

    <!-- Following Code from this source: -->
    <!-- https://stackoverflow.com/a/10282253 -->
    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

</resources>
